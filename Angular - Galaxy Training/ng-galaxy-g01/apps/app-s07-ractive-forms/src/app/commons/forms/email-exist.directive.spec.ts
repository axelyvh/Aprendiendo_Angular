import { EmailExistDirective } from './email-exist.directive';

describe('EmailExistDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailExistDirective();
    expect(directive).toBeTruthy();
  });
});
