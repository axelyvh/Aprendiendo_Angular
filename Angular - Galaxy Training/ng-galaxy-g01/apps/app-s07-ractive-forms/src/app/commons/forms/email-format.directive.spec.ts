import { EmailFormatDirective } from './email-format.directive';

describe('EmailFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailFormatDirective();
    expect(directive).toBeTruthy();
  });
});
